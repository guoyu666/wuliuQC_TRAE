# 物流QC周转筐收发记录小程序 - 规格说明书

## 1. 项目概述

- **项目名称**: 物流QC周转筐收发记录
- **项目类型**: 微信小程序
- **核心功能**: 记录库房每天蓝色框子和红色框子的发出和收回数量，包含线路和车辆信息
- **目标用户**: 库房管理人员、物流调度员

## 2. 技术架构

### 2.1 云开发架构
- **后端**: 微信云开发
- **数据库**: 云开发数据库（自动创建 users 和 records 集合）
- **云函数**: login（用户主动登录）、syncRecords（数据同步）
- **本地存储**: wx.setStorageSync（按微信账号隔离，记录写入通过串行队列落盘）
- **游客体验**: 每次冷启动都先进入本地体验，可直接浏览、记录、查询和统计，不要求手机号、头像或昵称
- **会话登录**: 持久化账号不会在下次启动自动激活，用户主动登录后仅在当前运行会话内开启云同步

### 2.2 数据同步策略
- **写入**: 本地优先，云端异步同步
- **读取**: 本地缓存优先，按时间游标增量同步；历史页使用云端游标分页
- **合并**: 多设备数据通过 syncRecords 云函数合并，以服务端 `serverRevision` 判定提交顺序
- **恢复**: 备份记录先写入独立 generation，完整校验后原子切换 active generation
- **在线状态**: App 前台生命周期每 30 秒发送一次心跳，90 秒内活跃账号计为在线

## 3. 功能列表

### 3.1 记录功能
- 记录线路名称（如：京沪线、沪宁线）
- 记录运送车辆车牌号
- 记录物流中心蓝色框子发出数量
- 记录物流中心红色框子发出数量
- 记录蓝色框子发出数量
- 记录蓝色框子收回数量
- 记录红色框子发出数量
- 记录红色框子收回数量
- 支持当天多次记录，每条记录独立保存
- 每条记录包含独立的线路、车牌、物流发出和收发数量

### 3.2 数据展示
- 今日收发汇总（当前日期的发出和收回总数）
- 当日记录条数统计
- 数据可视化柱状图
- 历史记录列表（按日期分组显示）

### 3.3 快捷操作
- 昨天快捷跳转
- 上周快捷跳转
- 月初快捷跳转

### 3.4 数据管理
- 本地数据持久化存储
- 云端数据同步（自动）
- 支持查看历史每天的收发数据
- 数据导出功能（包含线路、车牌信息）
- 编辑、删除、添加备注功能

## 4. UI/UX 设计方向

### 4.1 视觉风格
- 简洁实用的 Material Design 风格
- 蓝色为主色调（对应蓝色框子）
- 红色为辅助色（对应红色框子）

### 4.2 色彩方案
- 主色: #1976D2 (蓝色)
- 辅助色: #D32F2F (红色)
- 背景色: #F5F5F5
- 文字色: #333333
- 次要文字: #666666

### 4.3 布局方案
- **首页**: 日期选择器、快捷日期按钮、当日汇总卡片、数据可视化图表、新增记录表单
- **历史记录页**: 列表形式展示每日数据，支持分页、导出、编辑、删除
- **统计报表页**: 月度/年度汇总、收发对比图、趋势图

### 4.4 交互设计
- 文本输入使用标准输入框
- 数字输入使用减号和加号按钮
- 记录按钮使用明显的确认操作
- 操作完成后有Toast提示
- 日期切换带动画效果

## 5. 数据结构

### 5.1 本地记录项 (Local Record)
```json
{
  "id": "记录ID",
  "date": "2024-01-01",
  "routeName": "京沪线",
  "plateNumber": "京A12345",
  "sendBlueOut": 20,
  "sendRedOut": 15,
  "blueOut": 10,
  "blueIn": 5,
  "redOut": 8,
  "redIn": 3,
  "remark": "备注内容",
  "createTime": "2024-01-01 10:30:00",
  "updatedAt": 1710000000000,
  "serverRevision": 12,
  "synced": true
}
```

### 5.2 云端记录项 (Cloud Record)
```json
{
  "_id": "云端ID",
  "_openid": "用户OpenID",
  "id": "记录ID",
  "date": "2024-01-01",
  "routeName": "京沪线",
  "plateNumber": "京A12345",
  "sendBlueOut": 20,
  "sendRedOut": 15,
  "blueOut": 10,
  "blueIn": 5,
  "redOut": 8,
  "redIn": 3,
  "remark": "备注内容",
  "createTime": "Date",
  "updatedAt": 1710000000000,
  "deletedAt": 0,
  "serverRevision": 12,
  "generation": "legacy",
  "syncTime": "Date"
}
```

### 5.3 用户集合 (users)
```json
{
  "_id": "云端ID",
  "_openid": "用户OpenID",
  "nickname": "用户",
  "createTime": "Date",
  "lastLoginTime": "Date"
}
```

## 6. 页面结构

### 6.1 首页 (index)
- 日期选择器
- 快捷日期按钮（今天、昨天、上周、月初）
- 当日汇总卡片（显示蓝出、蓝入、红出、红入总数及记录条数）
- 数据可视化柱状图
- 新增记录表单（线路名称、车牌号、蓝/红框子收发数量、备注）
- 提交记录按钮
- 跳转历史记录入口
- 跳转统计报表入口

### 6.2 历史记录页 (history)
- 按日期倒序排列的记录列表
- 每日数据汇总显示（蓝出、蓝入、红出、红入总数）
- 每条记录显示：线路名称、车牌号、时间、收发数量、备注
- 分页加载（每页10条）
- 数据导出功能（包含线路、车牌信息）
- 编辑（可修改线路、车牌、数量）、删除、添加备注功能

### 6.3 统计报表页 (statistics)
- 月度/年度切换Tab
- 日期选择器（月份/年份）
- 上一月/上一年的导航按钮
- 当月/当年的快捷跳转
- 收发对比柱状图
- 每日/每月趋势图

### 6.4 欢迎页 (welcome)
- 用户主动打开的登录与数据说明页
- 支持继续本地体验或自愿登录开启云同步

## 7. 云函数列表

### 7.1 login 云函数
- **功能**: 用户主动登录后获取/创建账号记录
- **返回**: openid、是否新用户、用户信息

### 7.2 syncRecords 云函数
- **功能**: 数据同步、合并、清理
- **支持操作**:
  - `protocol`: 校验客户端与云函数协议版本
  - `upsert` / `delete`: 事务化写入记录并分配服务端修订号
  - `download`: 从云端拉取记录
  - `downloadChanges`: 按同步时间游标和 active generation 增量拉取，generation 变化时客户端自动回退全量快照
  - `historyPage`: 按业务日期和记录 ID 游标分页
  - `syncPending`: 仅上传本地待同步变更，返回单条失败结果
  - `merge`: 保留的旧协议兼容入口，新版启动同步不再全量上传
  - `restoreStart` / `restoreStatus` / `restoreChunk` / `restoreCommit` / `restoreAbort`: generation 化分片恢复、断点续传和云端取消
  - `cleanupGeneration`: 清理已停用的数据批次
  - `presence`: 更新应用级在线心跳并返回实时人数

## 8. 数据库集合

### 8.1 users 集合
- 存储用户基本信息
- 按 openid 索引

### 8.2 records 集合
- 存储所有收发记录
- 建议建立 `_openid + generation + date + _id` 复合索引，支持 active generation 历史游标分页
- 建议建立 `_openid + syncTime + _id` 复合索引，支持增量同步

### 8.3 userMeta 集合
- 存储字典、服务端修订号、active generation、恢复锁和在线心跳
- 建议建立 `key + lastSeenAt` 复合索引，支持在线人数统计

### 8.4 restoreJobs 集合
- 存储恢复批次上传进度和最终状态，便于失败诊断与重试

## 9. 项目文件结构

```
wuliuQC_TRAE/
├── app.js                 # 应用入口，包含云开发初始化和游客会话启动
├── app.json               # 应用配置，启用云能力
├── app.wxss               # 全局样式
├── project.config.json    # 项目配置，包含云函数目录
├── SPEC.md                # 本规格文档
├── sitemap.json           # SEO配置
├── utils/
│   ├── db.js              # 数据访问层（支持云+本地双模式）
│   └── util.js            # 工具函数
├── cloudfunctions/
│   ├── login/             # 登录云函数
│   │   ├── index.js
│   │   └── package.json
│   └── syncRecords/       # 数据同步云函数
│       ├── index.js
│       └── package.json
└── pages/
    ├── index/             # 首页
    ├── history/           # 历史记录
    ├── statistics/        # 统计报表
    └── welcome/            # 欢迎页
```

## 10. 已知限制

- 云开发免费额度有限（单用户1000并发）
- 部分手机可能不支持某些动画效果
